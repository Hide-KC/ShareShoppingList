<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:dist="http://schemas.android.com/apk/distribution"
  package="work.kcs_labo.share_shopping_list">

  <dist:module dist:instant="true" />

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application
    android:allowBackup="false"
    android:fullBackupContent="@xml/backup_descriptor"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

    <meta-data
      android:name="com.google.android.gms.ads.APPLICATION_ID"
      android:value="@string/ads_app_id" />

    <activity android:name=".activity.main.MainAct">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <action android:name="android.intent.action.VIEW" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".activity.auth.AuthAct">

    </activity>
    <activity android:name=".activity.new_todo_item.NewTodoItemAct">

    </activity>
    <activity android:name=".activity.new_todo_list.NewTodoListAct">

    </activity>
    <activity android:name=".activity.profile.ProfileAct">

    </activity>
    <activity android:name=".activity.todo_item_list.TodoItemListAct">

    </activity>

  </application>

</manifest>