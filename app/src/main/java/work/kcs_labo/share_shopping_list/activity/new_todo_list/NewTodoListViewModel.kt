package work.kcs_labo.share_shopping_list.activity.new_todo_list

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class NewTodoListViewModel(app: Application) : AndroidViewModel(app) {
}